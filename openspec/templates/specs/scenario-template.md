# Scenario: ${scenario-name}

## 1. 业务背景 (Context)
- 该场景是为了验证什么业务逻辑？

## 2. 初始状态 (Initial State)
- 前置条件：数据库状态、环境配置等。

## 3. 动作与输入 (Action & Input)
- 输入数据 (JSON/CLI 参数/API Request)
- 执行动作

## 4. 预期输出 (Expected Output)
- 响应结果 (JSON/Status Code)
- 副作用 (数据库变更、文件生成、消息发出)

## 5. 失败判定 (Failure Criteria)
- 哪些情况被视为验证失败？
